{
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.858.0",
    "@aws-sdk/lib-dynamodb": "^3.858.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "nodemon": "^3.1.10",
    "serverless-offline": "^14.4.0"
  },
  "scripts": {
  "dev": "nodemon --exec 'npx serverless offline'",
  "test": "jest",
  "test:watch": "jest --watch",
  "test:coverage": "jest --coverage"
}

}
